This folder stores the persisted vector database.

The vector database contains:
- Document embeddings
- Metadata
- Index structures for similarity search

The database is generated during the ingestion process and
used during retrieval to find relevant document chunks.